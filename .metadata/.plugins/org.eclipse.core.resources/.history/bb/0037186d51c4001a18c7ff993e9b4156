#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]){
	setvbuf(stdout, NULL, _IONBF, 0);
	setvbuf(stdout, NULL, _IONBF, 0);
	printf ("The nusdfdg is: %d\n", argc);
	return EXIT_SUCCESS;
}
